version: '2'
services:
  nginx:
    build:
      context: .
    ports:
      - '80:80'
    volumes:
      - ./.env:/app/.env
      - ./vhosts/nginx.conf:/opt/bitnami/nginx/conf/server_blocks/nginx.conf
volumes:
  mariadb_data:
    driver: local